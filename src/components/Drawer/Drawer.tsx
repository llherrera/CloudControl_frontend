import { useEffect } from 'react';
import { Drawer, Box, CssBaseline, Toolbar } from '@mui/material';
import MuiAppBar, { AppBarProps as MuiAppBarProps } from '@mui/material/AppBar';

// la idea de este componente es hacer un menu en la pagina de configuracion del plan para estructurar mejor esa pagina
// To Do
export const DrawerMenu = () => {
    return (
        <Box sx={{display: 'flex'}}>
            
        </Box>
    );
}
