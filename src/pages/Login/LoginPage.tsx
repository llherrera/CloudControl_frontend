import { LoginForm } from "@/components";
import { Header } from "@/components/Header";

export const LoginPage = () => {
    return (
        <Header>
            <LoginForm key={0}/>
        </Header>
    );
}