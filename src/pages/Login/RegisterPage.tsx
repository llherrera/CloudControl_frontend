import { Frame, RegisterForm } from '@/components';

import { useAppSelector } from '@/store';

export const RegisterPage = () => {

    return (
        <Frame>
            <RegisterForm/>
        </Frame>
    );
}