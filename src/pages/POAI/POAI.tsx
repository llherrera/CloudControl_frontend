import React from 'react'
import { Frame } from '@/components'

export const POAI = () => {
    return (
        <Frame
            data={<div>
                <h1>POAI</h1>
            </div>}
        />
    );
}