import React from "react";
import { Frame } from "@/components";

export const ProjectBank = () => {
    return (
        <Frame
            data={<div>
                <h1>ProjectBank</h1>
            </div>}
        />
    );
}